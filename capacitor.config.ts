import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.universalmediadownloader.app',
  appName: 'Universal Media Downloader',
  webDir: 'dist/renderer'
};

export default config;
